# MarketplaceTCG

Para instalar as dependencias, no Linux, rode:

```commandline
$ python3 -m venv nome_do_ambiente

$ source nome_do_ambiente/bin/activate

$ pip install -r requirements.txt
```


Para executar o projeto localmente, rode:

```commandline
$ ENV=development streamlit run app.py
```